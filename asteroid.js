AFRAME.registercomponent('asteroid',{
    init:function(){
        for (var i=1; i<=20, i++){
            //i
            var id=`asteroid$(i)`;

            //position variables
            var posX=math.floor(math.random()=1000+ 1000)
        }
    }
})
